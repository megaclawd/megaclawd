[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200
evm_version = "paris"

[profile.default.fmt]
line_length = 100
tab_width = 4

[rpc_endpoints]
mainnet = "${ETHEREUM_RPC_URL}"
base = "${BASE_RPC_URL}"
